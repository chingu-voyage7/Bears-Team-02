projects:
  app:
    schemaPath: src/schema.graphql
    includes: ['**/*.graphql']
    extensions:
      endpoints:
        default: http://localhost:7272
  prisma:
    schemaPath: 'src/generated/prisma.graphql'
    extensions:
      prisma: prisma.yml
